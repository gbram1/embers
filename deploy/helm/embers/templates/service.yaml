apiVersion: v1
kind: Service
metadata:
  name: {{ include "embers.fullname" . }}
  labels:
    {{- include "embers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "embers.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
