apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "embers.fullname" . }}-config
  labels:
    {{- include "embers.labels" . | nindent 4 }}
data:
  platform.yaml: |
    {{- .Values.platform | toYaml | nindent 4 }}
